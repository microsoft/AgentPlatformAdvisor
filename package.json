{
  "name": "agent-platform-advisor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "python3 scripts/golden_paths.py && npx playwright test",
    "test:golden": "python3 scripts/golden_paths.py",
    "test:e2e": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "serve": "^14.2.6"
  }
}
